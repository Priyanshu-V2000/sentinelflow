gatling {
  core {
    outputDirectoryBaseName = "sentinelflow-load-test"
    runDescription = "SentinelFlow Payment Fraud Detection Load Test"
  }
  http {
    connectTimeout = 10000
    readTimeout = 60000
  }
}
